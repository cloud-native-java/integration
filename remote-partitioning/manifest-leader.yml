---
applications:
- name: partition-master
  memory: 1G
  instances: 1
  host: partition-master-${random-word}
  path: target/remote-partitioning.jar
  services:
    - batch-mysql
    - batch-rabbitmq
  env:
    DEBUG: "true"
    SERVER_PORT: 80